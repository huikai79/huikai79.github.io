#!/usr/bin/env node
// Runtime candidate count is verified against the live Notion data source during implementation.
// This file intentionally contains no workspace snapshot because pending inbox rows are mutable user data.
console.log("external ingestion live pending-count verification is out-of-repo by design");
